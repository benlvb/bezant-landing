# Jekyll build output + local Bundler artifacts. Vercel builds `_site` fresh
# from the committed Gemfile.lock (see vercel.json), so none of this belongs
# in git.
_site/
.jekyll-cache/
.jekyll-metadata
vendor/
.bundle/
